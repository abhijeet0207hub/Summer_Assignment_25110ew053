#include<iostream>
using namespace std;
int main()
{
    int n,a,i=0,sum=0;
    cout<< "please enter the number";
    cin>> n;
    a=n;
    while (n>0)
    {
    i=n%10;
    n=n/10;

     sum=sum+i;
    }
    cout<< "the sum of digits of the  "<< a << " is "<< sum;
    return 0;
    
}
